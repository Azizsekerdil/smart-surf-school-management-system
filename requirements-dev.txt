# =============================================================================
# Development / QA dependencies
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
# =============================================================================
-r requirements.txt

pytest>=9.1.1                            # MIT
pytest-django>=4.14.0                     # BSD-3-Clause
pytest-cov>=7.1.0                        # MIT
pytest-xdist>=3.8.0                      # MIT
factory-boy>=3.3.3                       # MIT
Faker>=40.36.0                            # MIT
freezegun>=1.5.5                         # Apache-2.0
responses>=0.26.2                        # Apache-2.0

ruff>=0.16.3                              # MIT   (lint + format)
bandit>=1.9.4                            # Apache-2.0 (security static analysis)
pip-audit>=2.10.1                         # Apache-2.0 (dependency CVE audit)
django-debug-toolbar>=4.4              # BSD-3-Clause

# --- presentation generation (scripts/generate_presentation.py) --------------
# Windows-only for the PDF/PNG step: pywin32 drives PowerPoint over COM, which
# is the only converter available on the build machine. The PPTX files build
# without it; the PDF and HTML decks do not.
python-pptx>=1.0                       # MIT
pywin32>=306 ; sys_platform == "win32" # PSF-style
