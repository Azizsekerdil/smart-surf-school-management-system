"""Pluggable AI provider implementations.

Adding a provider means writing one class that implements
:class:`~apps.ai.providers.base.BaseAIProvider` and registering it in
:mod:`apps.ai.providers.registry`. Nothing else in the codebase changes.
"""
