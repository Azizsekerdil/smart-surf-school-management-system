"""In-product Help Center: the manual the school actually reads.

Every screen in the system has a matching help article written in English and
Turkish. Articles are Markdown in the database so a manager can correct a
procedure from the admin without a deploy — which is exactly why the rendered
output is sanitised before it reaches a browser.
"""
